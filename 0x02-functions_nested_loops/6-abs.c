#include "main.h"

/**
 * _abs - computes the absolute value of x
 * @x: the value to be evaluated
 *
 * Return: returns the absoulte value of x
 *
 */

int _abs(int x)
{
	if (x < 0)
	{
		return (x * -1);
	}
	else
	{
		return ();
	}
}
